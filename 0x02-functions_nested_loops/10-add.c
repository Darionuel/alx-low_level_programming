#include "main.h"
/**
 * add - adds two integers and returns the result
 * 
 * @i: integer added to j
 * @j: integer added to i
 *
 * Return: addition of integers
 */
int add(int i, int j)
{
	int k = i + j;

	return (k);
}
