#include "main.h"
/**
 * swap_int - swaps the values of two integers.
 * @a: pointer arg to an integer
 * @b: pointer arg to an integer
 *
 * Return: nothing
 */
void swap_int(int *a, int *b)
{
	*a = 42;
	*b = 98;
}
